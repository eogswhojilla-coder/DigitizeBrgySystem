[phases.setup]
nixPkgs = ["php82", "php82Extensions.gd", "php82Packages.composer", "nodejs"]

[start]
cmd = "php artisan migrate --force && php artisan storage:link && php artisan serve --host=0.0.0.0 --port=$PORT"
